using System;

namespace Novus.Features.Weather;

// Domain Model localized to this feature
public record WeatherForecast(DateOnly Date, int TemperatureC, string? Summary)
{
    public int TemperatureF => 32 + (int)(TemperatureC / 0.5556);
}
